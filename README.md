# collab-sphere-monorepo

# Prerequisites

- Node.js >= 22
- npm >= 9
- PostgreSQL (Can be run locally or as container. This project is using postgres container as database)
- Docker / Podman
- OpenSSL (optional, required only if you want to generate self-signed SSL certificates for product deployment)
